import { SelectCard } from './select-card';

describe('SelectCard', () => {
  it('should create an instance', () => {
    const directive = new SelectCard();
    expect(directive).toBeTruthy();
  });
});
